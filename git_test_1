
git log --pretty=format:"%an" -- path/to/directory | sort | uniq -c | sort -nr

git log --diff-filter=A -- path/to/directory

 git config --global user.name “Sandy1GC"

git config --global user.email whatever@mail.com

git config --global core.editor code

git config --list

git branch -f main HEAD~1

git checkout HEAD~4

git branch -f some_branch HEAD~3

git branch -f

git branch -f main HEAD^

git branch -f main C7

git checkout -b Bobby

git checkout HEAD^

git branch -f command

git reset HEAD~1
git revert HEAD

git cherry-pick C3 C4 C7

git rebase -i main
git rebase main_2 main

git rebase -i HEAD~2
git commit --amend
git rebase -i HEAD~2
git rebase caption main

git describe C1

git checkout HEAD~^2~2

git checkout HEAD~
git checkout HEAD^2
git checkout HEAD~2

git checkout Joe
git cherry-pick C4 C3 C2
git checkout Bobby
git cherry-pick C5 C4 C3 C2
git branch -f Monty C2

git pull
git pull --rebase

git branch -f main original/main
git checkout -b feature C2
git push origin feature 

git pull --rebase
git push

git checkout -b foo o/main

git branch -u o/main foo



